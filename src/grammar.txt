code        : statement (NEWLINE statement)* EOF

statement   : expr

expr        : term ((PLUS|MINUS) term)*

term        : factor ((MUL|DIV) factor)*

factor      : (PLUS|MINUS) power

power       : atom (POW factor)*

atom        : INT|FLOAT
            : LPAREN expr RPAREN